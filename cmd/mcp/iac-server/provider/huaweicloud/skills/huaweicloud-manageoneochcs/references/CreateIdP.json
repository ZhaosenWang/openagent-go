{
  "name": "CreateIdP",
  "summary": "创建身份提供商",
  "tags": "安全管理",
  "productshort": "ManageOneOCHCS",
  "region_id": "cn-north-4",
  "paths": {
    "/mo/api/moauth/v1/idps/{name}": {
      "put": {
        "tags": [
          "安全管理"
        ],
        "operationId": "CreateIdP",
        "summary": "创建身份提供商",
        "x-resource": "身份提供源管理",
        "description": "删除身份提供商信息。\n\ncurl命令调用示例:\n```\ncurl -v -H \"Content-Type:application/json\" -X GET \"https://ip:port/mo/api/moauth/v1/idps/{name}\"\n```",
        "x-filter": "ManageOne,interservice,erservice",
        "x-plane": "OM",
        "produces": [
          "application/json"
        ],
        "consumes": [
          "application/json"
        ],
        "parameters": [
          {
            "name": "x-auth-token",
            "in": "header",
            "required": true,
            "type": "string",
            "description": "接口支持的用户token。运营管理员,机机账号权限,VDC管理员权限,vdc业务员权限,vdc只读权限用户的token。"
          },
          {
            "name": "name",
            "in": "path",
            "description": "身份提供源名称",
            "required": true,
            "type": "string"
          },
          {
            "name": "identityProvider",
            "in": "body",
            "description": "创建身份提供源结构体",
            "required": true,
            "schema": {
              "$ref": "#/definitions/IdentityProvider"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "创建成功"
          }
        },
        "x-support-sdk": "N"
      }
    }
  },
  "definitions": {
    "IdentityProvider": {
      "properties": {
        "description": {
          "type": "string",
          "description": "描述。"
        },
        "protocol": {
          "type": "string",
          "description": "协议。"
        },
        "parameters": {
          "type": "string",
          "description": "参数。"
        },
        "policies": {
          "type": "string",
          "description": "策略。"
        },
        "expandParam": {
          "description": "身份提供商的授权映射的列表",
          "schema": {
            "$ref": "#/definitions/ExpandParam"
          }
        }
      }
    },
    "ExpandParam": {
      "properties": {
        "isDefaultRedirect": {
          "type": "boolean",
          "description": "是否默认跳转。"
        },
        "idpScope": {
          "type": "string",
          "description": "管理侧或者租户侧,枚举值为tenant(租户侧),management(管理侧)。"
        }
      }
    }
  },
  "version": "2.0",
  "parameters": {},
  "host": "hcs.cn-north-4.myhuaweicloud.com",
  "base_path": "/",
  "info_version": "v1",
  "method": "put",
  "uri": null
}