{
  "name": "UpdateInferIntranetConnection",
  "summary": "变更内网申请",
  "tags": "内网接入管理",
  "productshort": "ModelArts",
  "region_id": "af-north-1",
  "paths": {
    "/v2/{project_id}/intranet-connection/{id}": {
      "put": {
        "tags": [
          "内网接入管理"
        ],
        "summary": "变更内网申请",
        "description": "本接口用于对当前租户的内网接入申请进行状态变更操作,支持通过(APPROVE)、拒绝(REJECT)、取消(CANCEL)和重试(RETRY)等操作。适用于需要管理内网接入申请审批流程的场景。调用此接口前,确保已具备相应的变更权限,并提供有效的内网申请ID和所需的操作类型。变更成功后,内网申请的状态将更新为指定的操作结果,并记录相关日志。如果提供的内网申请ID无效、操作类型不支持或权限不足,将返回相应的异常信息,提示用户检查输入数据的有效性和权限。",
        "operationId": "UpdateInferIntranetConnection",
        "parameters": [
          {
            "name": "project_id",
            "in": "path",
            "description": "**参数解释:**\n用户项目ID。\n**约束限制:**\n不涉及。\n**取值范围:**\n不涉及。\n**默认取值:**\n不涉及。",
            "required": true,
            "type": "string"
          },
          {
            "name": "id",
            "in": "path",
            "description": "**参数解释:**\n内网接入id。id可以根据[查询当前租户的内网接入申请列表](ListInferIntranetConnectionApplications.xml)返回body的id字段得到。\n**约束限制:**\n不涉及。\n**取值范围:**\n不涉及。\n**默认取值:**\n不涉及。",
            "required": true,
            "type": "string"
          },
          {
            "in": "body",
            "name": "UpdateInferIntranetConnectionRequestBody",
            "required": false,
            "schema": {
              "$ref": "#/definitions/UpdateIntranetConnectionRequestBody"
            },
            "description": "**参数解释:**\n变更内网申请请求体。\n**约束限制:**\n不涉及。\n**取值范围:**\n不涉及。\n**默认取值:**\n不涉及。"
          },
          {
            "name": "x-auth-token",
            "in": "header",
            "description": "**参数解释:**\n用户Token。通过调用IAM服务获取用户Token接口获取响应消息头中X-Subject-Token的值。获取方法请参见[获取IAM用户Token(使用密码)](modelarts_03_0004.xml)。\n**约束限制:**\n不涉及。\n**取值范围:**\n不涉及。\n**默认取值:**\n不涉及。",
            "required": true,
            "type": "string"
          }
        ],
        "responses": {
          "204": {
            "description": "变更成功。"
          }
        },
        "x-request-examples-url-1": "PUT https://{endpoint}/v2/{project_id}/intranet-connection/{id}",
        "x-request-examples-text-1": {
          "action": "APPROVE",
          "reason": "ok"
        },
        "x-request-examples-1": {
          "action": "APPROVE",
          "reason": "ok"
        },
        "x-api-type": "open-api",
        "x-publish-doc": "Y",
        "x-open-region-cloud": "hk,hc,hcs,hcs_sm,fcs,hcso",
        "x-is-registered": "Y",
        "x-order": 1,
        "x-support-sdk": "Y"
      }
    }
  },
  "definitions": {
    "UpdateIntranetConnectionRequestBody": {
      "type": "object",
      "required": [
        "action"
      ],
      "properties": {
        "action": {
          "type": "string",
          "example": "APPROVE",
          "description": "**参数解释:**\n内网接入申请操作。\n**约束限制:**\n不涉及。\n**取值范围:**\n- APPROVE:通过申请。只有当内网接入申请处于“审批中”状态时,才可进行此操作。\n- REJECT: 拒绝申请。只有当内网接入申请处于“审批中”状态时,才可进行此操作。\n- CANCEL: 取消授权,只有当内网接入申请处于“通过”(CONNECTED)状态时,才可进行取消授权操作。\n- RETRY:  重试申请,只有当内网接入申请处于“异常”状态并且异常原因为“连接失败,请重试”时,才可进行重试操作。\n**默认取值:**\n不涉及。"
        },
        "reason": {
          "type": "string",
          "example": "ok",
          "description": "**参数解释:**\n拒绝时可以填入拒绝的原因。\n**约束限制:**\n不涉及。\n**取值范围:**\n不涉及。\n**默认取值:**\n不涉及。"
        }
      }
    }
  },
  "version": "2.0",
  "parameters": {},
  "host": "modelarts.af-north-1.myhuaweicloud.com",
  "base_path": "/",
  "info_version": "v1",
  "method": "put",
  "uri": null
}