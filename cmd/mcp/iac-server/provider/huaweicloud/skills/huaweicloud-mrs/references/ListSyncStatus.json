{
  "name": "ListSyncStatus",
  "summary": "查询当前集群是否在IAM同步进行中",
  "tags": "IAM同步管理接口",
  "productshort": "MRS",
  "region_id": "ae-ad-1",
  "paths": {
    "/v2/{project_id}/clusters/{cluster_id}/iam-sync": {
      "get": {
        "x-name": "查询同步状态",
        "x-support-sdk": "Y",
        "x-is-registered": "Y",
        "x-apiexplorer-hide": "N",
        "tags": [
          "IAMSyncManagement"
        ],
        "summary": "查询当前集群是否在IAM同步进行中",
        "description": "查询当前集群是否在IAM同步进行中",
        "operationId": "ListSyncStatus",
        "parameters": [
          {
            "name": "project_id",
            "in": "path",
            "required": true,
            "type": "string",
            "description": "项目ID",
            "pattern": "^[a-z0-9A-Z_-]*$",
            "maxLength": 64
          },
          {
            "name": "cluster_id",
            "in": "path",
            "required": true,
            "type": "string",
            "description": "集群ID",
            "pattern": "^[a-z0-9A-Z_-]*$",
            "maxLength": 64
          }
        ],
        "responses": {
          "200": {
            "description": "查询IAM同步任务状态的结果",
            "schema": {
              "$ref": "#/definitions/SynchronizationTaskStatus"
            },
            "examples": {
              "application/json": {
                "is_synchronizing": false
              }
            }
          }
        }
      }
    }
  },
  "definitions": {
    "SynchronizationTaskStatus": {
      "type": "object",
      "properties": {
        "is_synchronizing": {
          "type": "boolean",
          "description": "表示IAM同步任务是否正在进行中"
        }
      }
    }
  },
  "version": "2.0",
  "parameters": {},
  "host": "mrs.ae-ad-1.myhuaweicloud.com",
  "base_path": "/",
  "info_version": "v2",
  "method": "get",
  "uri": null
}