{
  "name": "ShowSyncIamUser",
  "summary": "获取已经同步的IAM用户和用户组",
  "tags": "IAM同步管理接口",
  "productshort": "MRS",
  "region_id": "ae-ad-1",
  "paths": {
    "/v2/{project_id}/clusters/{cluster_id}/iam-sync-user": {
      "get": {
        "x-name": "获取已经同步的IAM用户和用户组",
        "x-support-sdk": "Y",
        "x-is-registered": "Y",
        "x-apiexplorer-hide": "N",
        "tags": [
          "IAMSyncManagement"
        ],
        "x-tag": "IAMSync",
        "summary": "获取已经同步的IAM用户和用户组",
        "description": "获取已经同步的IAM用户和用户组",
        "operationId": "ShowSyncIamUser",
        "parameters": [
          {
            "name": "project_id",
            "in": "path",
            "required": true,
            "type": "string",
            "description": "项目ID",
            "pattern": "^[a-z0-9A-Z_-]*$",
            "maxLength": 64
          },
          {
            "name": "cluster_id",
            "in": "path",
            "required": true,
            "type": "string",
            "description": "集群ID",
            "pattern": "^[a-z0-9A-Z_-]*$",
            "maxLength": 64
          }
        ],
        "responses": {
          "200": {
            "description": "已经同步了的用户和用户组",
            "schema": {
              "$ref": "#/definitions/SynchronizedUsersAndGroups"
            },
            "examples": {
              "application/json": {
                "user_names": [
                  "user1",
                  "user2"
                ],
                "group_names": [
                  "group1"
                ]
              }
            }
          }
        }
      }
    }
  },
  "definitions": {
    "SynchronizedUsersAndGroups": {
      "type": "object",
      "description": "已经同步了的用户和用户组",
      "properties": {
        "user_names": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64
          },
          "description": "已经同步了的用户"
        },
        "group_names": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64
          },
          "description": "已经同步了的用户组"
        }
      }
    }
  },
  "version": "2.0",
  "parameters": {},
  "host": "mrs.ae-ad-1.myhuaweicloud.com",
  "base_path": "/",
  "info_version": "v2",
  "method": "get",
  "uri": null
}